# @ionify/ionify-darwin-x64

The macOS x64 native binding selected by `@ionify/ionify`. Install the main
package; this package is installed automatically as an optional dependency on
matching systems.
