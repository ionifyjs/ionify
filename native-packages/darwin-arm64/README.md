# @ionify/ionify-darwin-arm64

The macOS ARM64 native binding selected by `@ionify/ionify`. Install the main
package; this package is installed automatically as an optional dependency on
matching systems.
